# راهنمای نهایی - دسترسی به داشبورد

## 🚀 سریع‌ترین روش

### فقط این فایل را اجرا کنید:
```
start-all.bat
```

این Script:
- ✅ Backend را روی Port 8002 راه‌اندازی می‌کند
- ✅ Frontend را روی Port 3002 راه‌اندازی می‌کند
- ✅ پنجره‌های جداگانه برای هر سرویس باز می‌کند

**10-15 ثانیه صبر کنید** و سپس:
```
http://localhost:3002
```
را در Browser باز کنید.

---

## 🌐 آدرس‌های دسترسی

- **Frontend Dashboard**: http://localhost:3002
- **Backend API**: http://localhost:8002
- **Health Check**: http://localhost:8002/health
- **API Docs**: http://localhost:8002/api/docs

---

## 📱 استفاده

1. **باز کردن**: http://localhost:3002
2. **وارد کردن URL**: مثلاً `https://example.com`
3. **کلیک روی "شروع تحلیل"**
4. **مشاهده Dashboard**: بعد از شروع تحلیل

---

## ⚠️ اگر خطا می‌بینید

### ERR_CONNECTION_REFUSED
این یعنی سرویس‌ها در حال اجرا نیستند.

**راهکار:**
1. فایل `start-all.bat` را اجرا کنید
2. یا به صورت دستی راه‌اندازی کنید (راهنمای کامل: `MANUAL_START.md`)

### Port در حال استفاده
اگر Port 8002 یا 3002 اشغال است:
- در `start-all.bat` Port را تغییر دهید
- یا Process را Kill کنید:
  ```bash
  netstat -ano | findstr :8002
  taskkill /PID <PID> /F
  ```

---

## 🔍 بررسی وضعیت

### Backend
```
http://localhost:8002/health
```
باید ببینید: `{"status":"healthy"}`

### Frontend
```
http://localhost:3002
```
باید صفحه اصلی را ببینید.

---

## 📝 پورت‌ها

- **Backend**: 8002 (به دلیل اشغال بودن 8000 و 8001)
- **Frontend**: 3002 (به دلیل اشغال بودن 3000)

---

**لطفاً `start-all.bat` را اجرا کنید و سپس http://localhost:3002 را باز کنید!** 🚀

