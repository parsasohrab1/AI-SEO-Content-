# رفع مشکل 404 - Dashboard یافت نشد

## 🔴 مشکل

خطاهای 404 در Console:
- `Failed to load resource: the server responded with a status of 404`
- `/dashboard/{analysis_id}` یافت نشد
- `/dashboard/{analysis_id}/competitor-keywords` یافت نشد
- `/dashboard/{analysis_id}/analyze-competitors` یافت نشد

## 🔍 علت

1. **Backend Restart شده**: Dashboard در حافظه (memory) ذخیره می‌شود و با restart از بین می‌رود
2. **تحلیل جدید ایجاد نشده**: Dashboard فقط بعد از ایجاد تحلیل جدید ساخته می‌شود
3. **Endpointهای جدید**: ممکن است نیاز به restart Backend باشد

## ✅ راه‌حل

### مرحله 1: بررسی وضعیت Backend

```powershell
# بررسی Health
Invoke-WebRequest -Uri "http://localhost:8002/health"

# باید این پاسخ را ببینید:
# {"status":"healthy",...}
```

### مرحله 2: راه‌اندازی مجدد Backend (در صورت نیاز)

**روش 1: استفاده از Script**
```powershell
.\start-backend.ps1
```

**روش 2: دستی**
```powershell
cd backend
.\venv\Scripts\activate
uvicorn main:app --reload --host 0.0.0.0 --port 8002
```

### مرحله 3: ایجاد تحلیل جدید

1. به صفحه اصلی بروید: http://localhost:3002
2. URL سایت را وارد کنید
3. روی "شروع تحلیل" کلیک کنید
4. منتظر بمانید تا تحلیل کامل شود (10-15 ثانیه)
5. سپس به صفحه "تحلیل رقبا" بروید

## 📝 نکات مهم

- ✅ Dashboard در حافظه ذخیره می‌شود (با restart از بین می‌رود)
- ✅ برای استفاده از تحلیل رقبا، باید یک تحلیل جدید ایجاد کنید
- ✅ Endpointهای جدید بعد از restart Backend فعال می‌شوند

## 🔄 چک‌لیست

- [ ] Backend روی Port 8002 در حال اجرا است
- [ ] Frontend روی Port 3002 در حال اجرا است
- [ ] یک تحلیل جدید ایجاد شده است
- [ ] تحلیل کامل شده است (status: completed)
- [ ] به صفحه "تحلیل رقبا" رفته‌اید

## 🆘 اگر مشکل ادامه داشت

1. **بررسی لاگ‌های Backend**: در پنجره PowerShell که Backend را اجرا می‌کند
2. **بررسی Console مرورگر**: F12 → Console
3. **بررسی Network Tab**: F12 → Network → بررسی درخواست‌های 404

