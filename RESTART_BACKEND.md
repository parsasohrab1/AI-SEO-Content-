# راه‌اندازی مجدد Backend

## ⚠️ مشکل: Dashboard یافت نشد (404)

اگر خطای 404 می‌بینید، احتمالاً Backend restart شده و داده‌های Dashboard از بین رفته است.

## 🔄 راه‌حل: راه‌اندازی مجدد Backend

### روش 1: استفاده از PowerShell Script

```powershell
.\start-backend.ps1
```

یا دوبار کلیک روی فایل `start-backend.ps1`

### روش 2: راه‌اندازی دستی

```powershell
cd backend
.\venv\Scripts\activate
uvicorn main:app --reload --host 0.0.0.0 --port 8002
```

## ✅ پس از راه‌اندازی

1. **یک تحلیل جدید ایجاد کنید** از صفحه اصلی
2. **منتظر بمانید** تا تحلیل کامل شود
3. **سپس** به صفحه "تحلیل رقبا" بروید

## 📝 نکات مهم

- Dashboard در حافظه (memory) ذخیره می‌شود
- با restart شدن Backend، تمام Dashboard‌ها از بین می‌روند
- برای استفاده از قابلیت تحلیل رقبا، باید یک تحلیل جدید ایجاد کنید

## 🔍 بررسی وضعیت Backend

```powershell
# بررسی Health
Invoke-WebRequest -Uri "http://localhost:8002/health"

# بررسی API Docs
Start-Process "http://localhost:8002/api/docs"
```

