# راه‌اندازی سریع - AI Content Factory Pro

## ⚡ سریع‌ترین روش (Windows)

### فقط این فایل را اجرا کنید:
```
start-all.bat
```

این Script به صورت خودکار:
- ✅ Backend را روی http://localhost:8000 راه‌اندازی می‌کند
- ✅ Frontend را روی http://localhost:3002 راه‌اندازی می‌کند
- ✅ پنجره‌های جداگانه برای هر سرویس باز می‌کند

---

## 🌐 دسترسی

بعد از اجرای `start-all.bat`:

1. **Frontend Dashboard**: http://localhost:3002
2. **Backend API**: http://localhost:8000
3. **API Docs**: http://localhost:8000/api/docs

---

## ⚠️ اگر Port 8000 اشغال است

اگر محصول دیگری روی Port 8000 در حال اجرا است:

### گزینه 1: متوقف کردن سرویس دیگر
```bash
# پیدا کردن Process
netstat -ano | findstr :8000

# Kill کردن (PID را از خروجی بالا بگیرید)
taskkill /PID <PID> /F
```

### گزینه 2: تغییر Port Backend

1. فایل `start-backend.bat` را باز کنید
2. `--port 8000` را به `--port 8001` تغییر دهید
3. در فایل‌های Frontend (`app/page.tsx` و `app/dashboard/[id]/page.tsx`):
   - `http://localhost:8000` را به `http://localhost:8001` تغییر دهید

---

## 🔍 بررسی وضعیت

### Backend
باز کنید: http://localhost:8000/health

باید ببینید:
```json
{
  "status": "healthy",
  "service": "AI Content Factory Pro"
}
```

### Frontend
باز کنید: http://localhost:3002

باید صفحه اصلی را ببینید.

---

## 📝 پیش‌نیازها

- ✅ Python 3.11+ (`python --version`)
- ✅ Node.js 20+ (`node --version`)
- ✅ Port 8000 آزاد (یا تغییر دهید)
- ✅ Port 3002 آزاد

---

## 🆘 مشکلات؟

برای راهنمای کامل رفع مشکلات: `TROUBLESHOOTING.md`

---

**موفق باشید!** 🚀

